CREATE TABLE [dbo].[Table] (
    [ID]              NVARCHAR (50) NOT NULL,
    [Password]        INT           NULL,
    [Confirmpassword] INT           NULL,
    PRIMARY KEY CLUSTERED ([ID] ASC)
);

